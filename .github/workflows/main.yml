# .github/workflows/deploy.yml

name: Deploy URL File to GitHub Pages

# Run this workflow every time a change is pushed to the `main` branch
on:
  push:
    branches: [ "main" ]

# Allow this job to clone the repo and create a page deployment
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      # 1. Check out the repository content
      - name: Checkout
        uses: actions/checkout@v4

      # 2. Configure GitHub Pages
      - name: Setup Pages
        uses: actions/configure-pages@v4

      # 3. Upload the entire repository as an artifact
      #    This is what will be deployed to your site.
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Path defaults to the repository root, which is what we want.
          path: '.' 

      # 4. Deploy the artifact to GitHub Pages
      #    This step is extremely fast.
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
